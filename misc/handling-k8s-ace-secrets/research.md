# general ID
0. mqsicredential or mqsisetdbparms in config files
1. ace vault + mqsicredentials in startup script
2. ace vault base 64 in git, add as config in ace operator cr
3. ace vault manual upload in k8s, mapped to file with config map
4. ace vault with pass in env and startup script to load creds in env variables for custom nodes or connections
5. credentials in hashicorp vault and startup strict ace + trigger ace restart

# startup script



# ace vault cr




# config map vault





# hashicorp